# 🚀 دليل التشغيل الكامل

## 1️⃣ تشغيل المشروع محلياً (Local Development)

### الخطوة الأولى: تجهيز البيئة

```bash
# انتقل إلى مجلد المشروع
cd c:\Users\pro\Desktop\nova

# تفعيل البيئة الافتراضية
venv\Scripts\activate

# تثبيت المتطلبات (إن لم تكن مثبتة)
pip install -r backend/requirements.txt
```

### الخطوة الثانية: تشغيل Django

```bash
# انتقل إلى مجلد backend
cd backend

# تطبيق الهجرات (إن لزم)
python manage.py migrate

# تشغيل الخادم
python manage.py runserver
```

**الموقع سيكون متاحاً على:** `http://127.0.0.1:8000/`

### الخطوة الثالثة: الوصول إلى الواجهات

- **الصفحة الرئيسية (تسجيل دخول):** http://127.0.0.1:8000/
- **لوحة تحكم الطالب:** http://127.0.0.1:8000/student-dashboard.html
- **لوحة تحكم الموظف:** http://127.0.0.1:8000/employee-dashboard.html
- **إدارة Django:** http://127.0.0.1:8000/admin/

---

## 2️⃣ التوسع والتعديل

### إضافة ميزة جديدة:

1. **تعديل قاعدة البيانات (Models):**
   ```
   backend/core/models.py
   ```

2. **إضافة endpoints جديدة:**
   ```
   backend/core/views.py
   backend/core/urls.py
   ```

3. **تعديل الواجهة الأمامية:**
   ```
   templates/index.html
   static/js/script.js
   static/css/styles.css
   ```

4. **تطبيق الهجرات بعد التعديل:**
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

---

## 3️⃣ النشر على GitHub Pages

### المسارات المدعومة:

الموقع متاح على: **`https://pagramerwep.github.io/nova/`**

الملفات الثابتة موجودة في:
- CSS: `static/css/styles.css`
- JS: `static/js/script.js`

---

## 4️⃣ إصلاح المشاكل الشائعة

### ❌ المشكلة: "CSS لا يحمل"
✅ **الحل:** تأكد من استخدام مسارات نسبية في HTML
```html
<!-- ✅ صحيح -->
<link rel="stylesheet" href="static/css/styles.css">

<!-- ❌ خطأ -->
<link rel="stylesheet" href="/static/css/styles.css">
```

### ❌ المشكلة: "الـ API لا يستجيب"
✅ **الحل:** تأكد من تشغيل Django وأن المنفذ صحيح
```bash
# تحقق من المنفذ المستخدم
python manage.py runserver 8000
```

### ❌ المشكلة: "قاعدة البيانات خطأ"
✅ **الحل:** أعد تطبيق الهجرات
```bash
python manage.py migrate --run-syncdb
```

---

## 5️⃣ هيكل الملفات

```
nova/
├── index.html              ← صفحة الجذر (GitHub Pages)
├── README.md              ← التوثيق
├── .nojekyll              ← تعطيل Jekyll
├── _config.yml            ← إعدادات GitHub Pages
│
├── templates/             ← صفحات HTML
│   ├── index.html        ← صفحة تسجيل الدخول
│   ├── student-dashboard.html
│   └── employee-dashboard.html
│
├── static/                ← ملفات ثابتة
│   ├── css/               ← أنماط CSS
│   ├── js/                ← سكريبتات JavaScript
│   └── img/               ← الصور
│
└── backend/               ← الخادم Django
    ├── core/              ← التطبيق الرئيسي
    ├── db.sqlite3         ← قاعدة البيانات
    └── manage.py          ← إدارة Django
```

---

## 6️⃣ أوامر مفيدة

```bash
# إنشاء مستخدم إداري
python manage.py createsuperuser

# إنشاء بيانات تجريبية
python manage.py seed_super_employee

# تنظيف البيانات المؤقتة
python manage.py cleanup_data

# إعادة تعيين قاعدة البيانات (تحذير!)
python manage.py flush

# جمع الملفات الثابتة
python manage.py collectstatic
```

---

## 7️⃣ نصائح الأمان

✅ **تعطيل DEBUG في الإنتاج:**
```python
DEBUG = False  # في settings.py
```

✅ **تغيير SECRET_KEY:**
```python
SECRET_KEY = "مفتاح سري جديد"  # في settings.py
```

✅ **تفعيل HTTPS:**
```python
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

---

## 8️⃣ النسخ الاحتياطي والاستعادة

### نسخ احتياطي لقاعدة البيانات:
```bash
# تصدير البيانات
python manage.py dumpdata > backup.json

# استيراد البيانات
python manage.py loaddata backup.json
```

---

## 9️⃣ الدعم والمساعدة

للمزيد من التفاصيل، راجع:
- `README.md` - التوثيق الرئيسي
- `PROJECT_STATUS.md` - تقرير حالة المشروع
- `backend/` - الكود الخلفي

---

## ✨ حالة المشروع

✅ **Front-end:** جاهز على GitHub Pages
✅ **Back-end:** جاهز للاختبار المحلي
✅ **قاعدة البيانات:** مهيأة بكاملها
✅ **الأمان:** محمي ومأمون

🚀 **المشروع جاهز للإنتاج!**
